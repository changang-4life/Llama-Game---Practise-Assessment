""" Llama Game
v2 - Import Llama
"""

import pygame
import time

pygame.init() # initialises pygame so it's ready to use

llama_sprite = pygame.image.load("Llama.png") # loads the llama sprite in
screen = pygame.display.set_mode((1100, 550)) # sets up the screen display

pygame.quit()
quit()
