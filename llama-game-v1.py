""" Llama Game
Jade Akinbo
v1 - Establish Game Display
"""

import pygame
import time

pygame.init() # initialises pygame so it's ready to use

screen = pygame.display.set_mode((1000, 720)) # sets up the screen display

time.sleep(5) # wait period for testing purposes

pygame.quit()
quit()